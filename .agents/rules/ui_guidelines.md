# UI Guidelines

1. **Icons Only**: App wide, only show icons for buttons and actions. Do not use text labels next to icons unless absolutely necessary.
2. **Tooltips**: Always include descriptive tooltips (e.g. using a `title` attribute or a tooltip component) for all icons to explain their function to the user.
